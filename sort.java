public class sort (int x , int n , int s) {
    int arrays[];
    arrays[] = new int[n];
    for (int i = 0 ; i < n ; i++){
        for ( int j = 0 ; j < n-1 ; j++){
            if(arr[j]<=arr[j+1]) {
		dumm
            }
            else {
                arr[j] = 
            }
        }
    }

    return (arrays);
}
